name: "Docker runtime audit"

on:
  schedule:
    - cron: "0 1 * * 1"
  workflow_dispatch:

jobs:
  audit:
    name: Audit itg-redmine mcp Docker runtime
    runs-on: mcp
    timeout-minutes: 10
    steps:
      - name: Verify Docker runtime controls
        uses: vilnius-vmsa/shared-workflows/.github/actions/check-docker-runtime@main
